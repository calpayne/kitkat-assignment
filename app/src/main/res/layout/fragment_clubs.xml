<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorWhite" >

    <EditText
        android:id="@+id/searchText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/search_clubs_button"
        android:layout_toStartOf="@+id/search_clubs_button"
        android:hint="SEARCH..."
        android:layout_margin="5dp" />

    <ImageButton
        android:id="@+id/search_clubs_button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentEnd="true"
        android:background="@drawable/ic_search"
        android:layout_margin="5dp"
        android:contentDescription="@string/search" />

    <RelativeLayout
        android:layout_marginTop="56dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <Button
            android:id="@+id/btn_my_clubs"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:text="@string/my_clubs"
            android:textColor="@color/colorWhite"
            android:layout_margin="5dp"
            android:background="@drawable/circular_background" />

        <ListView
            android:id="@+id/clubs_list_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentBottom="true"
            android:layout_alignParentStart="true" />

    </RelativeLayout>
  
</RelativeLayout>